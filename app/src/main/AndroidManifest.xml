<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.edu.fatecjahu.imcapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ImcApp">
        <activity
            android:name=".ResultadoActivity"
            android:parentActivityName=".EntradaActivity"
            android:exported="true" />
        <activity
            android:name=".EntradaActivity"
            android:parentActivityName=".BemVindoActivity"
            android:exported="true" />
        <activity
            android:name=".BemVindoActivity"
            android:parentActivityName=".MainActivity"
            android:exported="true" />
        <activity
            android:name=".MainActivity"
            android:exported="true" />
        <activity
            android:name=".SplashScreenActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>